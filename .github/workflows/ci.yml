name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.name }}
    runs-on: windows-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows PowerShell 5.1
            shell: powershell
          - name: PowerShell 7
            shell: pwsh

    env:
      CI_SHELL: ${{ matrix.shell }}
    defaults:
      run:
        shell: pwsh -NoProfile -Command "& $env:CI_SHELL -NoLogo -NoProfile -NonInteractive -File '{0}'; exit $LASTEXITCODE"

    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install PSScriptAnalyzer
        run: |
          $ProgressPreference = 'SilentlyContinue'
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          Install-Module `
            -Name PSScriptAnalyzer `
            -RequiredVersion 1.25.0 `
            -Scope CurrentUser `
            -Force

      - name: Run static tests
        run: .\tests\Test-Static.ps1

      - name: Run release tests
        run: .\tests\Test-Release.ps1

      - name: Run PSScriptAnalyzer
        run: |
          $results = @(
            Invoke-ScriptAnalyzer `
              -Path .\Watch-ChromeRam.ps1 `
              -Settings .\PSScriptAnalyzerSettings.psd1
            Invoke-ScriptAnalyzer `
              -Path .\tests\Test-Static.ps1 `
              -Settings .\PSScriptAnalyzerSettings.psd1
            Invoke-ScriptAnalyzer `
              -Path .\tests\Test-Release.ps1 `
              -Settings .\PSScriptAnalyzerSettings.psd1
            Invoke-ScriptAnalyzer `
              -Path .\tools\Build-Release.ps1 `
              -Settings .\PSScriptAnalyzerSettings.psd1
          )
          $results | Format-Table -AutoSize
          if ($results.Count -gt 0) {
            throw "PSScriptAnalyzer reported $($results.Count) issue(s)."
          }

      - name: Build release archive
        run: .\tools\Build-Release.ps1 -Version 0.3.0
